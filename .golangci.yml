version: "2"

linters:
  default: none
  enable:
    - loglint
  settings:
    custom:
      loglint:
        type: module
        description: Checks slog and zap log messages for style and possible sensitive data.